---
import Layout from '../../layouts/Layout.astro';
---

<Layout 
    title="AI顧問｜株式会社コールテン"
    description="紹介限定・長期前提のAI顧問。事業理解と最新知見をもとに、Slack/定例で伴走しながら、クライアントに合うAI活用を設計・検証・定着させます。"
    canonicalUrl="https://corduroy.co.jp/services/advisory"
    ogUrl="https://corduroy.co.jp/services/advisory"
>
    <script slot="head" type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "Service",
      "serviceType": "AI顧問",
      "provider": {
        "@type": "Organization",
        "name": "株式会社コールテン",
        "url": "https://corduroy.co.jp/"
      },
      "name": "AI顧問",
      "description": "紹介限定・長期前提のAI顧問。事業理解と最新知見をもとに、Slack/定例で伴走しながら、クライアントに合うAI活用を設計・検証・定着させます。",
      "url": "https://corduroy.co.jp/services/advisory",
      "areaServed": {
        "@type": "Country",
        "name": "Japan"
      }
    }
    </script>
    <script slot="head" type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": "BreadcrumbList",
      "itemListElement": [
        {
          "@type": "ListItem",
          "position": 1,
          "name": "ホーム",
          "item": "https://corduroy.co.jp/"
        },
        {
          "@type": "ListItem",
          "position": 2,
          "name": "AI顧問",
          "item": "https://corduroy.co.jp/services/advisory"
        }
      ]
    }
    </script>

    <main>
        <section class="hero">
          <div class="container hero-inner">
            <div class="fade-up">
              <div class="icon-lg">🤝</div>
              <h1>AI顧問</h1>
              <p>事業の「手触り」を大切にしながら、最新のAI知見をあなたのチームに溶け込ませます。</p>
              <div style="display:flex; gap:12px; justify-content:center; flex-wrap:wrap; margin-top:16px;">
                <span class="pill">紹介限定</span>
                <span class="pill">Slack相談無制限</span>
                <span class="pill">定例 MTG</span>
              </div>
            </div>
          </div>
        </section>
    
        <section class="container">
          <div class="section-head fade-up">
            <small>Features</small>
            <h2>AI顧問ができること</h2>
          </div>
          <div class="grid cols-3">
            <div class="card fade-up">
              <h3>活用戦略と設計</h3>
              <p class="mini">「どこにAIを入れると効果的か」を、現場の負荷やデータの質を考慮しながら一緒に描き出します。</p>
            </div>
            <div class="card fade-up">
              <h3>ツール・技術選定</h3>
              <p class="mini">乱立するAIツールの中から、コスト・セキュリティ・使い勝手のバランスが最適なものを選びます。</p>
            </div>
            <div class="card fade-up">
              <h3>プロンプト・検証</h3>
              <p class="mini">実務で使える精度のプロンプトを作成し、出力の安定性や品質を泥臭く検証し続けます。</p>
            </div>
          </div>
        </section>
    
        <section class="bg-gradient-soft">
          <div class="container">
            <div class="section-head fade-up">
              <small>Process</small>
              <h2>伴走スタイル</h2>
            </div>
            <div class="grid cols-3">
              <div class="card fade-up">
                <div class="pill" style="margin-bottom:16px; align-self:flex-start;">Day to Day</div>
                <h3>Slackでの即時相談</h3>
                <p class="mini">「このAIツールどう？」といった軽い相談から、プロンプトの添削まで日常的にサポート。</p>
              </div>
              <div class="card fade-up">
                <div class="pill" style="margin-bottom:16px; align-self:flex-start;">Bi-weekly</div>
                <h3>定例アップデート</h3>
                <p class="mini">隔週〜月1で進捗を確認。最新ニュースの中から、あなたの事業に関係あるものだけを抜粋して共有します。</p>
              </div>
              <div class="card fade-up">
                <div class="pill" style="margin-bottom:16px; align-self:flex-start;">On-demand</div>
                <h3>現場トレーニング</h3>
                <p class="mini">必要に応じて、メンバーが自走できるよう勉強会やハンズオンを実施します。</p>
              </div>
            </div>
          </div>
        </section>
    
        <section class="container">
          <div class="section-head fade-up">
            <small>Pricing</small>
            <h2>料金目安</h2>
          </div>
          <div class="grid cols-2">
            <div class="card fade-up">
              <h3>スタンダード顧問</h3>
              <p class="mini">Slack相談無制限 ＋ 定例MTG（月2回）。戦略設計から実務支援までバランスよく行います。</p>
              <h2 style="margin-top:auto; margin-bottom:0; font-size:28px;">月額 15万円〜</h2>
            </div>
            <div class="card fade-up">
              <h3>プロジェクト限定</h3>
              <p class="mini">新規事業の立ち上げや、特定のシステム刷新に合わせて、期間を区切って集中的に支援します。</p>
              <h2 style="margin-top:auto; margin-bottom:0; font-size:28px;">個別お見積もり</h2>
            </div>
          </div>
          <p class="mini" style="text-align:center; margin-top:40px; color:var(--text-light); font-size:14px;">
            ※企業の規模や関与度合いに応じて調整可能です。</p>
        </section>
    
        <section class="container faq">
          <div class="section-head fade-up">
            <small>FAQ</small>
            <h2>よくあるご質問</h2>
          </div>
          <div class="fade-up">
            <details open>
              <summary>他のコンサルティングと何が違いますか？</summary>
              <p class="mini">「レポートを出して終わり」ではなく、Slackで日常的に対話し、自らもプロンプトを書く「実務者」として伴走する点が特徴です。</p>
            </details>
            <details>
              <summary>契約期間に縛りはありますか？</summary>
              <p class="mini">長期的な活用を目指すため基本は6ヶ月〜ですが、最初の2ヶ月間はトライアル期間として解約可能な形を取ることも可能です。</p>
            </details>
            <details>
              <summary>Slack以外のツールでも対応可能ですか？</summary>
              <p class="mini">Teams, Chatwork, Discordなど、貴社の社内ツールに合わせて柔軟に対応いたします。</p>
            </details>
          </div>
        </section>
    
        <section class="container">
          <div class="section-head fade-up">
            <small>Client Voices</small>
            <h2>利用者の声</h2>
            <p>AI顧問サービスを利用されているクライアントのインタビューを掲載予定です。</p>
          </div>
          <div class="fade-up" style="max-width:960px;margin:0 auto;">
            <div class="card"
              style="text-align:center;padding:60px 24px;background:#f9fafb;border:2px dashed #d7e6de; align-items:center; justify-content:center;">
              <h3 style="color:#7a8f87;margin-bottom:8px;">📝 準備中</h3>
              <p class="mini" style="color:#7a8f87;">AI顧問サービスを利用されているクライアントの<br>インタビュー記事を近日公開予定です。</p>
            </div>
          </div>
        </section>
    
        <section id="contact" class="bg-contact">
          <div class="container">
            <div class="section-head fade-up">
              <small>Contact</small>
              <h2>ご相談はこちらから</h2>
              <p>ご相談をご希望いただける場合はこちらのコンタクトページで詳細をご確認ください。</p>
            </div>
            <div class="card fade-up" style="max-width:680px; margin:0 auto; text-align:center; align-items:center;">
              <p class="mini" style="margin-bottom:20px;">公式フォームはありません。コンタクトページの案内に沿って、SNSのDMなどからご連絡ください。</p>
              <a class="btn" href="/contact" style="padding:16px 28px;">コンタクトページを見る →</a>
            </div>
          </div>
        </section>
      </main>
</Layout>

<style>
    /* Hero Section */
    .hero {
      padding: 160px 0 140px;
      /* Organic/Flow Pattern: Larger, Warmer, distinct "Conversation" bubbles */
      background-color: var(--cream);
      background-image:
        radial-gradient(circle at 10% 20%, rgba(22, 97, 78, 0.08) 0%, transparent 40%),
        radial-gradient(circle at 90% 80%, rgba(22, 97, 78, 0.08) 0%, transparent 40%),
        radial-gradient(circle at 50% 50%, rgba(212, 160, 77, 0.15) 0%, transparent 60%),
        radial-gradient(circle at 80% 10%, rgba(212, 160, 77, 0.1) 0%, transparent 30%),
        radial-gradient(circle at 20% 90%, rgba(22, 97, 78, 0.05) 0%, transparent 30%);
      text-align: center;
      position: relative;
      overflow: hidden;
    }

    /* Subtle texture overlay for Hero */
    .hero::before {
      content: "";
      position: absolute;
      inset: 0;
      background-image: url("data:image/svg+xml,%3Csvg width='60' height='60' viewBox='0 0 60 60' xmlns='http://www.w3.org/2000/svg'%3E%3Cg fill='none' fill-rule='evenodd'%3E%3Cg fill='%2316614e' fill-opacity='0.03'%3E%3Cpath d='M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E");
      opacity: 0.5;
      mask-image: linear-gradient(to bottom, black 0%, transparent 100%);
      -webkit-mask-image: linear-gradient(to bottom, black 0%, transparent 100%);
      pointer-events: none;
    }

    .hero-inner {
      display: flex;
      flex-direction: column;
      align-items: center;
      gap: 40px;
      position: relative;
      z-index: 1;
    }

    .hero h1 {
      margin-bottom: 32px;
      background: linear-gradient(135deg, var(--ink) 0%, var(--sea) 60%, var(--ink) 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      position: relative;
      text-shadow: 0 20px 40px rgba(22, 97, 78, 0.1);
    }

    .hero p {
      font-size: 20px;
      color: var(--text);
      max-width: 760px;
      margin: 0 auto 40px;
      line-height: 2;
      font-weight: 400;
    }

    section {
      padding: 100px 0;
      position: relative;
    }

    /* Section Headers */
    .section-head {
      text-align: center;
      margin-bottom: 72px;
      position: relative;
    }

    .section-head small {
      display: inline-block;
      color: var(--accent);
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.15em;
      margin-bottom: 16px;
      font-family: "Space Grotesk", sans-serif;
      font-size: 12px;
    }

    .section-head p {
      max-width: 600px;
      margin: 0 auto;
    }

    /* Modern Card Design */
    .card {
      background: #fff;
      border: 1px solid rgba(22, 97, 78, 0.08);
      border-radius: 24px;
      padding: 40px;
      box-shadow: var(--shadow-sm);
      transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
      position: relative;
      overflow: hidden;
      display: flex;
      flex-direction: column;
      height: 100%;
    }

    .card::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      height: 6px;
      background: linear-gradient(90deg, var(--mint) 0%, var(--sea) 50%, var(--mint) 100%);
      opacity: 0;
      transition: opacity 0.4s ease;
    }

    .card:hover {
      transform: translateY(-8px);
      box-shadow: var(--shadow-lg);
      border-color: rgba(22, 97, 78, 0.2);
    }

    .card:hover::before {
      opacity: 1;
    }

    .card h3 {
      font-size: 24px;
      margin-bottom: 16px;
      color: var(--ink);
    }

    .card .mini {
      font-size: 16px;
      color: var(--text-light);
      line-height: 1.8;
      margin-bottom: 0;
    }

    .grid {
      display: grid;
      gap: 32px;
    }

    @media(min-width:768px) {
      .grid.cols-3 {
        grid-template-columns: repeat(3, 1fr)
      }

      .grid.cols-2 {
        grid-template-columns: repeat(2, 1fr)
      }
    }

    /* FAQ Styling */
    .faq details {
      border: 1px solid var(--line);
      border-radius: 20px;
      padding: 24px 32px;
      background: #fff;
      margin-bottom: 16px;
      transition: all 0.3s ease;
      box-shadow: var(--shadow-sm);
    }

    .faq details:hover {
      border-color: var(--sea);
      box-shadow: var(--shadow-md);
    }

    .faq details[open] {
      border-color: var(--sea);
      background: #fff;
      box-shadow: var(--shadow-md);
      padding-bottom: 32px;
    }

    .faq summary {
      cursor: pointer;
      font-weight: 700;
      color: var(--ink);
      font-size: 18px;
      padding: 4px 0;
      transition: color 0.2s ease;
      list-style: none;
      position: relative;
      padding-right: 48px;
    }

    .faq summary::-webkit-details-marker {
      display: none;
    }

    .faq summary::after {
      content: '+';
      position: absolute;
      right: 0;
      top: 50%;
      transform: translateY(-50%);
      font-size: 28px;
      color: var(--sea);
      font-weight: 300;
      transition: transform 0.3s cubic-bezier(0.16, 1, 0.3, 1);
    }

    .faq details[open] summary::after {
      transform: translateY(-50%) rotate(45deg);
    }

    .faq details[open] summary {
      border-bottom: 1px solid var(--line);
      margin-bottom: 24px;
      padding-bottom: 16px;
    }

    .faq summary:hover {
      color: var(--sea);
    }

    .icon-lg {
      font-size: 64px;
      margin-bottom: 32px;
      color: var(--sea);
      line-height: 1;
      display: inline-flex;
      align-items: center;
      justify-content: center;
      width: 120px;
      height: 120px;
      background: linear-gradient(135deg, #fff 0%, var(--mint) 100%);
      border-radius: 40px;
      border: 1px solid var(--white);
      box-shadow:
        0 16px 32px rgba(22, 97, 78, 0.1),
        inset 0 2px 4px rgba(255, 255, 255, 1);
      position: relative;
      transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
    }

    .bg-gradient-soft {
      background: linear-gradient(180deg, var(--cream) 0%, #f1f7f5 100%);
      position: relative;
    }

    .bg-gradient-soft::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background:
        radial-gradient(circle at 10% 20%, rgba(22, 97, 78, 0.02) 0%, transparent 40%),
        radial-gradient(circle at 90% 80%, rgba(22, 97, 78, 0.02) 0%, transparent 40%);
      pointer-events: none;
    }

    .bg-contact {
      background: linear-gradient(135deg, #f0f7f4 0%, #e6f3ed 100%);
      position: relative;
      overflow: hidden;
    }

    .bg-contact::before {
      content: '';
      position: absolute;
      top: -50%;
      left: -50%;
      width: 200%;
      height: 200%;
      background: radial-gradient(circle, rgba(22, 97, 78, 0.03) 0%, transparent 60%);
      animation: rotateBg 20s linear infinite;
    }

    @keyframes rotateBg {
      from { transform: rotate(0deg); }
      to { transform: rotate(360deg); }
    }

    .pill {
      padding: 12px 20px;
      border-radius: 100px;
      background: #fff;
      border: 1px solid var(--line);
      box-shadow: var(--shadow-sm);
      font-weight: 600;
      color: var(--text);
      font-size: 14px;
      display: inline-flex;
      align-items: center;
      gap: 8px;
    }

    @media(max-width:768px) {
      section { padding: 60px 0; }
      .hero { padding: 80px 0 60px; }
      .hero h1 { font-size: 34px; }
      .hero p { font-size: 16px; }
      .grid.cols-2, .grid.cols-3 { grid-template-columns: 1fr; }
    }
</style>
